# Core
requests>=2.31.0
pyyaml>=6.0
tqdm>=4.66.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

# Behavioral track (OpenRouter is plain HTTP, no special SDK required)

# Mechanistic track (install these only where you have a GPU, e.g. Colab)
torch>=2.1.0
transformers>=4.40.0
transformer_lens>=1.17.0
sae_lens>=3.0.0
einops>=0.7.0
